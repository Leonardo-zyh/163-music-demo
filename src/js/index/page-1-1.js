{
    let view = {
        
    }
    let model = {}
    let controller = {
        init(view,model){
            this.view=view
            this.model=model
            //this.bindEvents()
            //this.bindEventHub()
        }
    }
    controller.init(view,model)
}